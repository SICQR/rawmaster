# RAWMASTER Full — Gradio Web App
# Deploy to HuggingFace Spaces or Railway

# ── Web framework ──────────────────────────────────────────
gradio>=5.0.0

# ── Audio processing ───────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# ── Mastering / loudness ───────────────────────────────────
pyloudnorm>=0.1.1
noisereduce>=3.0.0

# ── Stem separation ────────────────────────────────────────
demucs>=4.0.0

# ── MIDI extraction ────────────────────────────────────────
# basic-pitch has tricky deps — install without extras to avoid TF/CoreML conflicts:
#   pip install basic-pitch --no-deps
#   pip install mir-eval pretty-midi resampy onnxruntime
basic-pitch>=0.3.0
mir-eval>=0.7.0
pretty-midi>=0.2.9
resampy>=0.4.2
onnxruntime>=1.16.0
